# Processor / sub-processor register

- **Status:** in force — personal open-source project, 2026-08-08 (ADR 0005)
- **Owner:** Rosa (Legal & Privacy Officer / privacy contact)
- **Legal basis:** GDPR art. 28 (processors), Cap. V (transfers), art. 30 (records of processing activities)
- **Review:** annually and on any provider change; additions require the privacy/legal owner's approval before onboarding.

> **Disclaimer:** this document is product guidance / not legal advice. The register is in force for the pilot jurisdiction (Italy); per-jurisdiction review remains the documented precondition for an EU-wide launch.

---

## 1. Register

| # | Processor | Service | Data processed | Role | DPA / mechanism | Transfer & residency | Sub-processors | Status |
|---|-----------|---------|----------------|------|-----------------|----------------------|----------------|--------|
| PR1 | **Cloudflare, Inc.** | Cloudflare Workers (compute/edge) + D1 (database, binding `DB`) + **Email Routing (transactional email for account verification / password reset — `opensurveillancedb.org`, AUTH MULTI-METODO Fase A2)** | All application data: pending/verified records, notes, moderation decisions, correction requests, contributor accounts/sessions. **Transactional email** (verification tokens, password reset): only the recipient address and the tokenised link, **zero tracking pixels/links** (ADR 0020). **Photo feature retired (2026-08-08, CEO):** the R2 binding (`PHOTOS`) is removed from `wrangler.jsonc`, no new writes occur; existing objects in the bucket `opensurveillancedb-photos` are **retained without deletion** (no processing of new photo data). | **Processor** (art. 28) | Cloudflare Data Processing Addendum (**DPA v6.3, June 2025**) incorporating **EU SCCs (2021/914)** (modules 2/3 as applicable) — the DPA covers all Cloudflare services under the account, **including Email Routing** | Transfers EU→US possible via edge; mitigation: D1 configured with EU residency (**`weur` location hint**); encryption in transit (TLS); supplementary-measures assessment documented in § 2; **Cloudflare participates in the EU–US Data Privacy Framework (DPF-certified)** — DPF is an additional transfer ground alongside the SCCs | Per Cloudflare's published sub-processor list (DPA § list, change-notified) — reviewed at contracting | Onboarded — **Cloudflare DPA v6.3 incorporated in Cloudflare's Terms of Service (accepted at account setup)**; region pinning: D1 `weur` set |
| PR2 | ~~OpenAI, LLC~~ — **retired (2026-08-08):** the ChatGPT sign-in scaffold (`app/chatgpt-auth.ts`, headers `oai-authenticated-user-*`) was removed from the codebase; identity providers are now **GitHub / Google OIDC** (PR5/PR6) | — | — | — | — | — | — | **Retired** — superseded by PR5/PR6 |
| PR3 | **GitHub, Inc.** | Source repository hosting | Source code only; no runtime data, no personal data | Not a processor in practice (no OSDB data processed) | GitHub ToS | n/a | n/a | Informational |
| PR4 | **OpenStreetMap Foundation / tile.openstreetmap.org** | Map tiles (test only) | None (public map tiles) | No personal data | OSM tile usage policy | n/a | n/a | Test only; production provider TBD (review H5) |
| PR5 | **GitHub, Inc. (OIDC identity provider)** | Contributor sign-in via GitHub OAuth/OIDC (AUTH MULTI-METODO Fase D) | Identity attributes received at login: provider subject id (`external_sub`), display name, verified flag — **no email imported, never logged** | **Dormant — not a processor while OIDC is off.** If activated (Fase D), a **processor** of the identity attributes exchanged at login (art. 28) | GitHub's **DPA + EU–US DPF** certification must be verified and recorded **before activation** (activation gate, ADR 0020; the provider's own OAuth service is controller-to-controller) | US (GitHub); the login event and the caller's IP are visible to GitHub — disclosed in the UI risk matrix and PRIVACY_NOTICE § 3.1/§ 6; assessed under DPF/SCC at activation | n/a (provider side) | **Conditional — dormant** until the operator configures GitHub credentials (`ops/oidc-secrets.sh`; DPA + DPF + register flip). Distinct from PR3 (source-repo hosting, informational). |
| PR6 | **Google LLC (OIDC identity provider)** | Contributor sign-in via Google OIDC (AUTH MULTI-METODO Fase D) | Identity attributes received at login: provider subject id (`external_sub`), display name, verified flag — **no email imported, never logged** | **Active on the pre-prod deployment** (Google credentials configured; PR5 analog for GitHub). Processor (art. 28) of the identity attributes exchanged at login | Google's **DPA + EU–US DPF** certification recorded at activation (activation gate, ADR 0020; the provider's own OAuth service is controller-to-controller) | US (Google); the login event and the caller's IP are visible to Google — disclosed in the UI risk matrix and PRIVACY_NOTICE § 3.1/§ 6; assessed under DPF/SCC | n/a (provider side) | **Active** — Google OIDC configured (2026-08-08); flip back to dormant if credentials are removed. |

## 2. International transfers (Cap. V GDPR) — assessment summary

### Cloudflare (PR1)
- **Transfer instrument:** Cloudflare DPA (**v6.3, June 2025**) with **EU Standard Contractual Clauses (2021/914)** — controller-to-processor and processor-to-processor modules as applicable; the DPA is incorporated in Cloudflare's Terms of Service, accepted at account setup by the controller (**Simone Rondina (syax89) / OpenSurveillanceDB**). **EU–US Data Privacy Framework:** Cloudflare is DPF-certified; the DPF is recorded as an additional transfer ground (the SCCs alone are sufficient; DPF strengthens the transfer assessment).
- **Supplementary measures (TIA summary):** data at rest in D1 pinned to `weur` (Western Europe); TLS for all traffic; Workers edge execution minimises data transfer (dynamic app, not bulk data); Cloudflare's published sub-processor list and incident-notification commitments accepted in the DPA. The retired R2 photo storage (PR #64) no longer receives writes — the retained objects are covered by the same Cloudflare DPA; no new transfer assessment needed (no new photo data).
- **Residual risk:** low for the dataset described (mostly non-personal infrastructure data + pseudonymous contributor metadata). Reassessed annually.

### OpenAI (PR2 — retired 2026-08-08)
- The ChatGPT sign-in scaffold was **removed from the codebase**; it was never wired to a live page. Identity providers are now **GitHub / Google OIDC** (PR5/PR6). No OSDB data was ever sent to OpenAI. The "never log emails" rule (M4) now applies to the OIDC providers' identity attributes (which are limited to subject id + display name + verified flag — no email to log).

### GitHub / Google as OIDC identity providers (PR5/PR6 — Google active, GitHub conditional)
- **Google (PR6) is active on the pre-prod deployment** (credentials configured via `ops/oidc-secrets.sh`); GitHub (PR5) stays dormant until its credentials are configured. The exchange is limited to the authentication moment: the provider authenticates the contributor and we receive only the subject id, display name and the verified flag — **no email, no onward transfer of OSDB data**. The transfer assessment: US-based providers, covered by their DPA (SCCs) and by **EU–US Data Privacy Framework certification** (recorded at activation for Google); the residual privacy cost is the provider's own observation of the login event and the caller's IP, disclosed in the login UI risk matrix (Fase E2) and PRIVACY_NOTICE § 3.1/§ 5/§ 6.

## 3. Safeguards and commitments

- **No email logging:** application logs and audit logs must never contain contributor emails (M4; enforced in code). **Same rule for OIDC:** no provider email is ever imported (PR5/PR6 — the email column keeps a deterministic non-routable placeholder, RFC 2606), so there is nothing to log.
- **Onboarding rule:** no new processor/sub-processor may be onboarded without a DPA review and privacy/legal owner sign-off (art. 28(2)). **OIDC activation gate (ADR 0020):** GitHub/Google (PR5/PR6) move from *conditional* to *active* only after their DPA + EU–US DPF certification are verified and recorded, and the register row is flipped — never before.
- **Data residency:** D1 `location_hint = "weur"` before any real data load.
- **Photo storage (retired):** the photo upload feature was removed 2026-08-08 (CEO). The R2 `PHOTOS` binding is gone from `wrangler.jsonc`; existing objects are retained without deletion and no new writes occur — the old storage-hygiene and deletion obligations no longer apply to new data (see PR1).
- **Backup retention:** D1 automatic backups (hourly, 24 h) and Time Travel PITR (30 days) are accepted as the technical erasure horizon (RETENTION_SCHEDULE.md R10); no long-term export backups of D1 are configured.
- **Breach notification:** Cloudflare's DPA commits the processor to notifying us per art. 33(2) without undue delay; our procedure is BREACH_PROCEDURE.md. GitHub's / Google's sign-in incidents are governed by their own incident commitments — no OSDB data at risk there (the exchange is limited to identity attributes at the authentication moment, PR5/PR6).

## 4. Open items before launch

- [ ] Execute Cloudflare DPA (SCC) with the controller (**Simone Rondina (syax89) / OpenSurveillanceDB**); pin D1 region `weur`.
- [x] **R2 photo bucket region/jurisdiction — CLOSED (2026-08-08):** the photo upload feature was removed; the `PHOTOS` binding is gone from `wrangler.jsonc` and no new writes occur. The existing bucket `opensurveillancedb-photos` is left untouched (retained objects, no deletion) and needs no EU jurisdiction pinning (no processing of new photo data).
- [ ] **Confirm the applicable SCC version at DPA execution:** the Commission has announced a new generation of SCCs (public consultation Q4 2024; adoption announced for 2025) to replace the 2021/914 clauses. The register and the DPA must reference the SCC version in force at signature.
- [x] ~~Wire ChatGPT auth to `/moderation` (fixes H1)~~ — **resolved 2026-08-08:** the ChatGPT scaffold was removed; moderators authenticate as verified contributors (roles `moderator`/`admin`, db/users.ts) with the same methods as any contributor.
- [x] **OIDC activation gate (ADR 0020, Fase D — owner: legal/CTO):** resolved for **Google** on the pre-prod deployment (DPA + EU–US DPF recorded, PR6 flipped to *active*, 2026-08-08). **GitHub (PR5) stays conditional** until its credentials are configured (`ops/oidc-secrets.sh`) and the same gate is recorded. PRIVACY_NOTICE § 5/§ 6 and TERMS § 3.7 describe the method as "active where the operator has configured the provider".
- [ ] Confirm whether Cloudflare's sub-processor list changes require an updated review at contract signature.
- [ ] Choose and assess the production map-tile provider (H5).
- [ ] Revise this register and PRIVACY_NOTICE § 5 per ADR 0012: the pilot runtime is self-hosted (Italy) and Cloudflare returns to a future precondition — to be completed before the pilot hosts real data.
